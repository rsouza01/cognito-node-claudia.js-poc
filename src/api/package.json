{
  "name": "auth-api-demo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "create-deploy": "claudia create --region $AWS_REGION  --api-module api --name $npm_package_name",
    "update-api": "claudia update --region AWS_REGION",
    "destroy-api": "claudia destroy --region AWS_REGION"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "claudia-api-builder": "^4.1.2"
  }
}
